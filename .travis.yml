language: ruby
cache: bundler
sudo: false
rvm: 2.2

install: script/bootstrap
script: script/build
deploy:
  provider: pages
  local-dir: _site
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  target-branch: master
  on:
    branch: develop

branches:
  only:
  - develop
